---
const { id } = Astro.params;
const decimalId = id !== undefined && /^\d+$/.test(id) ? Number(id) : NaN;
const validId = Number.isSafeInteger(decimalId) && decimalId > 0;

if (!validId) {
    Astro.response.status = 404;
} else {
    return Astro.redirect(`/rpg/clans/${decimalId}`, 301);
}
---
