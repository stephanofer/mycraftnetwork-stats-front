---
interface Props {
  username?: string;
}

const { username = "desconocido" } = Astro.props;
---

<div class="not-found-container">
  <div class="not-found-content">
    <div class="error-icon">
      <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24">
        <path fill="currentColor" d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10s10-4.48 10-10S17.52 2 12 2m0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3s-3-1.34-3-3s1.34-3 3-3m0 14.2a7.2 7.2 0 0 1-6-3.22c.03-1.99 4-3.08 6-3.08c1.99 0 5.97 1.09 6 3.08a7.2 7.2 0 0 1-6 3.22"/>
      </svg>
      <div class="error-badge">?</div>
    </div>

    <h1 class="error-title">Jugador no encontrado</h1>
    
    <p class="error-message">
      No pudimos encontrar a un jugador con el nombre <strong>"{username}"</strong>
    </p>

    <div class="suggestions">
      <p class="suggestions-title">Posibles razones:</p>
      <ul class="suggestions-list">
        <li>El nombre de usuario está mal escrito</li>
        <li>El jugador nunca ha entrado al servidor</li>
        <li>El jugador cambió su nombre de usuario</li>
      </ul>
    </div>

    <div class="actions">
      <a href="/" class="action-button primary">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24">
          <path fill="currentColor" d="M10 20v-6h4v6h5v-8h3L12 3L2 12h3v8z"/>
        </svg>
        Volver al inicio
      </a>
      <button type="button" class="action-button secondary" onclick="history.back()">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24">
          <path fill="currentColor" d="M20 11H7.83l5.59-5.59L12 4l-8 8l8 8l1.41-1.41L7.83 13H20z"/>
        </svg>
        Volver atrás
      </button>
    </div>
  </div>
</div>

<style>
  .not-found-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 200px);
    padding: 2rem;
  }

  .not-found-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 500px;
    animation: fadeIn 0.5s ease;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .error-icon {
    position: relative;
    color: var(--color-opacity);
    margin-bottom: 1.5rem;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 0.7;
    }
    50% {
      opacity: 1;
    }
  }

  .error-badge {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 28px;
    height: 28px;
    background: #ff4444;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.1rem;
    border: 3px solid #000;
  }

  .error-title {
    font-family: "Clash", sans-serif;
    font-size: var(--fs-xxl);
    font-weight: 600;
    color: white;
    margin: 0 0 1rem;
  }

  .error-message {
    font-size: var(--fs-base);
    color: var(--color-opacity);
    margin: 0 0 2rem;
    line-height: 1.6;
  }

  .error-message strong {
    color: var(--resalt);
    font-weight: 600;
  }

  .suggestions {
    width: 100%;
    padding: 1.25rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--color-divider);
    border-radius: 0.75rem;
    margin-bottom: 2rem;
    text-align: left;
  }

  .suggestions-title {
    font-size: var(--fs-sm);
    color: var(--color-opacity);
    margin: 0 0 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .suggestions-list {
    margin: 0;
    padding-left: 1.25rem;
    list-style: disc;
  }

  .suggestions-list li {
    font-size: var(--fs-sm);
    color: rgba(255, 255, 255, 0.7);
    padding: 0.25rem 0;
    list-style-type: disc;
  }

  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .action-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 0.75rem;
    font-family: "Inter", sans-serif;
    font-size: var(--fs-base);
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    text-decoration: none;
    border: none;
  }

  .action-button.primary {
    background: var(--resalt);
    color: white;
  }

  .action-button.primary:hover {
    background: #0084d0;
    transform: translateY(-2px);
  }

  .action-button.secondary {
    background: rgba(255, 255, 255, 0.05);
    color: white;
    border: 1px solid var(--color-divider);
  }

  .action-button.secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }
</style>
