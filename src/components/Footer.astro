<footer class="footer">
    <div class="shell">
        <div class="footer-main">
            <a
                class="footer-brand"
                href="/"
                aria-label="Mycraft Network, inicio"
            >
                <img src="/images/favicon.webp" alt="" width="38" height="38" />
                <span
                    ><strong>Mycraft Network</strong><small
                        >Estadísticas competitivas</small
                    ></span
                >
            </a>

            <p class="credits">
                Desarrollado por <a
                    href="https://stephanofer.com/"
                    rel="external noreferrer">Stephanofer</a
                >
            </p>

            <nav aria-label="Enlaces secundarios">
                <a href="/">Inicio</a>
                <a href="/ranking/rpg/kills">Rankings</a>
                <a href="/rpg/clans">Clanes</a>
                <a href="https://discord.mycraft.es" rel="external noreferrer"
                    >Discord</a
                >
            </nav>
        </div>
    </div>
</footer>

<style>
    .footer {
        border-top: 1px solid var(--line);
        background: #090b10;
        color: var(--muted);
    }
    .footer-main {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        min-height: 108px;
        align-items: center;
        gap: 2rem;
        padding-block: 1.5rem;
    }
    .footer-brand {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    .footer-brand img {
        width: 38px;
        height: 38px;
    }
    .footer-brand strong,
    .footer-brand small {
        display: block;
    }
    .footer-brand strong {
        color: var(--text);
        font-family: var(--display);
        font-size: 0.95rem;
        font-weight: 550;
        letter-spacing: 0.02em;
    }
    .footer-brand small {
        margin-top: 0.25rem;
        color: var(--faint);
        font-size: 0.68rem;
    }
    nav {
        display: flex;
        flex-wrap: wrap;
        justify-self: end;
        gap: 1.5rem;
        font-size: 0.78rem;
        font-weight: 650;
    }
    nav a {
        transition: color 160ms ease;
    }
    nav a:hover {
        color: var(--text);
    }
    .credits {
        margin: 0;
        color: var(--faint);
        font-size: 0.68rem;
        text-align: center;
    }
    .credits a {
        color: var(--muted);
        font-weight: 650;
        transition: color 160ms ease;
    }
    .credits a:hover {
        color: var(--text);
    }

    @media (max-width: 640px) {
        .footer-main {
            min-height: 0;
            grid-template-columns: 1fr;
            padding-block: 1.75rem;
        }
        .footer-brand,
        .credits,
        nav {
            justify-self: center;
        }
        .footer-brand {
            text-align: left;
        }
        nav {
            justify-content: center;
            gap: 1rem 1.35rem;
        }
    }
</style>
